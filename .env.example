# API base URL (without trailing /careers/)
# If empty, falls back to the Render deployment automatically.
# Set to http://127.0.0.1:8000 to use a local Django backend instead.
# VITE_API_BASE_URL=https://codeleap-engineering-test-ip1k.onrender.com
